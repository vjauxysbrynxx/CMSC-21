#include <stdio.h>
#include <stdbool.h>

    int main (void) {
        int age;
        bool teenager = false;

        if (age <= 19 && age >= 13)
            teenager = true;

        return 0;
    }